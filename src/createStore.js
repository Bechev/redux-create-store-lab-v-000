export default function createStore(reducer) {

  function dispatch(){
    state = 
  }

  function getState(){
    return state;
  }
}

function render() {
  const container = document.getElementById('container');
}
