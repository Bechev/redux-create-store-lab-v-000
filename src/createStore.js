export default function createStore(reducer) {

  function dispatch(){
    // state =
  }

  function getState(){
    return state;
  }

  return {
    dispatch,
    getState
  }
}

function render() {
  const container = document.getElementById('container');
}
